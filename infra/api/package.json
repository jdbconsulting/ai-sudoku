{
	"name": "ai-sudoku-api",
	"private": true,
	"version": "0.0.1",
	"description": "Lambda backend for the AI-Sudoku high score board.",
	"type": "module",
	"main": "handler.ts",
	"scripts": {
		"build": "tsc --noEmit",
		"lint": "tsc --noEmit"
	},
	"dependencies": {
		"@aws-sdk/lib-dynamodb": "^3.700.0",
		"esbuild": "^0.24.0"
	},
	"devDependencies": {
		"@aws-sdk/client-dynamodb": "^3.700.0",
		"@types/aws-lambda": "^8.10.149",
		"@types/node": "^22.10.0",
		"typescript": "^5.7.0"
	}
}
